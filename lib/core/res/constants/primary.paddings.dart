import 'package:flutter/material.dart';

abstract class PrimaryPaddings {
  static const standard = EdgeInsets.all(8);
  static const baseHorizontal = EdgeInsets.symmetric(horizontal: 22);
  static const smallHorizontal = EdgeInsets.symmetric(horizontal: 8);
}
