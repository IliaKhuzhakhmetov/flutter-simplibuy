import 'package:flutter/material.dart';

abstract class PrimaryRadiuses {
  static final base = BorderRadius.circular(8);
  static final middle = BorderRadius.circular(16);
  static final big = BorderRadius.circular(32);
}
